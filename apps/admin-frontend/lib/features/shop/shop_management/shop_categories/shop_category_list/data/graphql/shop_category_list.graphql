query shopCategories(
  $paging: OffsetPaging
  $filter: ShopCategoryFilter
  $sorting: [ShopCategorySort!]!
) {
  shopCategories(filter: $filter, sorting: $sorting, paging: $paging) {
    nodes {
      ...shopCategory
    }
    pageInfo {
      ...OffsetPageInfo
    }
    totalCount
  }
}
