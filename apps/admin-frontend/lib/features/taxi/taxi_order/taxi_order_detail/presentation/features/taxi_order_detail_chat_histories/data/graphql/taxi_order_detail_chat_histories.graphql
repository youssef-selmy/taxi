query getTaxiOrderDetailChatHistories($orderId: ID!) {
  order(id: $orderId) {
    driver {
      ...DriverName
    }
    rider {
      ...CustomerCompact
    }
    chatHistories: conversation {
      ...conversation
    }
  }
}
