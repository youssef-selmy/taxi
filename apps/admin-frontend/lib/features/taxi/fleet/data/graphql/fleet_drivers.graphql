query fleetDrivers(
  $paging: OffsetPaging
  $sorting: [DriverSort!]!
  $filter: DriverFilter!
) {
  drivers(filter: $filter, paging: $paging, sorting: $sorting) {
    nodes {
      ...driverListItem
    }
    pageInfo {
      ...OffsetPageInfo
    }
    totalCount
  }
}
