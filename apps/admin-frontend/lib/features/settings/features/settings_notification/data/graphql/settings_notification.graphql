query currentUserNotificationSettings {
  me {
    enabledNotifications
  }
}

mutation updateNotificationSettings($input: [EnabledNotification!]!) {
  updateProfile(input: { enabledNotifications: $input }) {
    enabledNotifications
  }
}
