query getParkingOrderSupportRequest($parkingId: IDFilterComparison!) {
  parkingOrderComplaints: parkingSupportRequests(filter: { id: $parkingId }) {
    nodes {
      ...parkingOrderSupportRequest
    }
  }
}
