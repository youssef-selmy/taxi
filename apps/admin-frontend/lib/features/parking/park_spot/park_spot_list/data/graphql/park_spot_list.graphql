query parkSpots(
  $filter: ParkSpotFilter
  $paging: OffsetPaging
  $sorting: [ParkSpotSort!]!
) {
  parkSpots(filter: $filter, paging: $paging, sorting: $sorting) {
    nodes {
      ...parkingListItem
    }
    pageInfo {
      ...OffsetPageInfo
    }
    totalCount
  }
}
