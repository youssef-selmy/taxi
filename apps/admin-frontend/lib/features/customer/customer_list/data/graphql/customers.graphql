query customersList(
  $paging: OffsetPaging
  $filter: RiderFilter
  $sorting: [RiderSort!]
) {
  customers: riders(paging: $paging, filter: $filter, sorting: $sorting) {
    ...CustomersListConnection
  }
}
