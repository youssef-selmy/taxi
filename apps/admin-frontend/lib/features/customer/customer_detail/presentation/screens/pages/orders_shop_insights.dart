import 'package:flutter/material.dart';

class OrdersShopInsights extends StatelessWidget {
  final String customerId;

  const OrdersShopInsights({super.key, required this.customerId});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
