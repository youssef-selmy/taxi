query customerShopReviews($customerId: ID!) {
  shopFeedbacks(filter: { customerId: { eq: $customerId } }) {
    nodes {
      ...shopFeedback
    }
    pageInfo {
      ...OffsetPageInfo
    }
    totalCount
  }
}
