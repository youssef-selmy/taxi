query customerAddresses($customerId: ID!) {
  addresses(filter: { riderId: { eq: $customerId } }) {
    nodes {
      ...Address
    }
    pageInfo {
      ...OffsetPageInfo
    }
    totalCount
  }
}
