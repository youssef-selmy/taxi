query customerTaxiReviews($customerId: ID!) {
  taxiReviews: feedbacks(
    filter: { request: { riderId: { eq: $customerId } } }
  ) {
    nodes {
      ...reviewTaxi
    }
    pageInfo {
      ...OffsetPageInfo
    }
    totalCount
  }
}
