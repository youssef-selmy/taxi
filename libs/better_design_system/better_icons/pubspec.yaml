name: better_icons
description: A package for Lume icons.
version: 0.1.0

resolution: workspace

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  build_runner: ^2.10.0
  icomoon_generator: ^1.0.0

flutter:
  fonts:
    - family: Icomoon
      fonts:
        - asset: font/icomoon.ttf
