import 'package:flutter/material.dart';

class EcommerceCheckoutMobile extends StatelessWidget {
  const EcommerceCheckoutMobile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
