name: permission_orchestrator
description: "Shared permission handling for Flutter apps with event-based checks and contextual UI"
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: "^3.35.0"

dependencies:
  flutter:
    sdk: flutter

  permission_handler: ^12.0.1
  geolocator: ^14.0.2
  firebase_messaging: ^16.0.3
  shared_preferences: ^2.5.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
